var a = 100, b = 3.14; // 숫자형(number type)
var c = "안녕하세요", d = "a"; // 문자열(string type)
var e = true, f = false; // 부울린형(boolean type)

console.log(a, typeof(a));
console.log(c, typeof(c));
console.log(e, typeof(e));
// JS에서는 정수와 실수를 따로 구분하지 않고 숫자형이라는 데이터 타입을 사용한다.
